const Null = {
  name: "null",
  description: "A null value represents a reference to an invalid or nonexistent object value. In Javascript, a null is marked as a primitive value even though it's typeof returns an object. It's different from an undefined value in that it has no identification or ability to point to anything.",
  example: "let number = null, null === undefined = false, null == undefined = true, !null = true, typeof null = object",
};

module.exports = Null;

